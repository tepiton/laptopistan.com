---
phase: live
updated: 2026-08-25
last_commit: 9e3b554
---

## Current Focus

Single-page static site generated by [Mimeo](https://github.com/pborenstein/mimeo), hosted on GitHub Pages. One `index.html` displaying one framed image; no build system, no other pages.

## Active Tasks

None open.

## Context

- Site is a minimal image-gallery frame (monospace, black/white, one photo via Flickr URL).
- `index.html` is hand-edited directly — no templating/build step in this repo.
- Layout is viewport-locked: `html, body` use `height: 100vh`/`100dvh` + `overflow: hidden`, so the page never scrolls, it clips instead.
- `index.html` was reformatted to readable multi-line CSS/HTML (was minified).
- History: Mimeo scaffold → embedded picture HTML → hosted Flickr URL → made it the front page → prettyprinted + locked to viewport height.

## Next Session

Pick up by reading this file and `git log --oneline -5`. If adding more pages/images or changing the layout system, note the rationale in a `DECISIONS.md` at that point.
